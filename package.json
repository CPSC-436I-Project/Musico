{
    "name": "musico",
    "version": "1.0.0",
    "engines": {
        "node": "12.x",
        "yarn": "1.x"
    },
    "scripts": {
        "heroku-prebuild": "cd app && yarn install && npm install && npm run build && cd ../server && npm install",
        "start": "cd ./server && npm start"
    }
}